; Ensure all PHP errors are written to stderr (visible in container/Railway logs).
; display_errors is off so errors never leak into HTTP responses.
[PHP]
display_errors = Off
log_errors = On
error_log = /dev/stderr
error_reporting = E_ALL
